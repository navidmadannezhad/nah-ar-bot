import { Menu } from "@grammyjs/menu";

export const GetNewIntervalMenu = new Menu("get-new-interval-menu")

    .text("فعال سازی", async (ctx: any) => {
        const selectedService = ctx.session.selectedService;
    });